# Assets

Place static assets here:

- `icons/`   — SVG icons and favicons
- `images/`  — Screenshots, hero images, og-image
- `fonts/`   — Self-hosted web fonts (if any)

Currently the project uses Unicode emoji icons and pure CSS/Canvas effects,
so no binary assets are required to run the site.
